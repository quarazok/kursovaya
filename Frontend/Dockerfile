# Берём готовый nginx — это веб-сервер,
# который умеет отдавать статические файлы
FROM nginx:alpine

# Копируем всю статику (HTML, CSS, JS, папку client/)
# в директорию, откуда nginx раздаёт файлы
COPY . /usr/share/nginx/html

# Порт 80 — стандартный для http внутри контейнера
EXPOSE 80
